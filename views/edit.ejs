<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>1990 Blog</title>
    <link rel="stylesheet" href="edit.css" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Jost:ital,wght@0,100..900;1,100..900&display=swap"
      rel="stylesheet"
    />

    <style>
      * {
        margin: 0;
        padding: 0;
      }

      body {
        margin: 30px;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: auto;
        font-family: "Jost", sans-serif;
      }

      .header {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        font-size: 20px;
      }

      body form {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
      }

      form .name {
        display: flex;
        flex-direction: column;
        margin: 20px 0;
      }

      form .write {
        display: flex;
        flex-direction: column;
        margin-bottom: 20px;
      }

      form .name input {
        width: 700px;
        height: 10px;
        padding: 10px;
        border: 1px solid black;
        border-radius: 3px;
      }

      form .write textarea {
        width: 700px;
        resize: vertical;
        padding: 10px;
        border: 1px solid black;
        border-radius: 3px;
      }

      form button {
        padding: 5px 12px;
        border: 1px solid black;
        border-radius: 3px;
        cursor: pointer;
      }
    </style>
  </head>
  <body>
    <div class="header">
        <h1>Edit Post</h1>
        <a href="/post">Back to Posts</a>
    </div>
    
    <form action="/post/<%= index %>?_method=PUT" method="post">
      <div class="name">
        <label for="name">Name:</label>
        <input type="text" placeholder="Input your name" name="name" value="<%= person %>" required/>
      </div>
      <div class="write">
        <label for="write">Your Writings:</label>
        <textarea placeholder="Write Here" name="write" required><%= post %></textarea>
      </div>

      <button type="submit">Update</button>
    </form>
    
  </body>
</html>
